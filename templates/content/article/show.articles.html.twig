{% extends 'base.admin.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <p class="mark">Pour trier écrire : 'ASC' or 'DESC' dans content and save</p>
    {{ include('content/_form.html.twig') }}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>Created_at</th>
                <th>Updated_at</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.title }}</td>
                <td>{{ article.content }}</td>
                <td>{{ article.createdAt ? article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ article.updatedAt ? article.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_article_show', {'id': article.id}) }}"><p class='btn'>show</p></a>
                    <a href="{{ path('app_article_edit', {'id': article.id}) }}"><p class='btn'>edit</p></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_article_create') }}"><p class="btn">Create new<p></a>
{% endblock %}
